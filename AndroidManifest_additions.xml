<!--
  Add these entries to android/app/src/main/AndroidManifest.xml,
  inside the <manifest> tag (permissions) and <application> tag
  (OneSignal notification icon/color, optional).
-->

<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!--
  Minimum SDK: OneSignal + geolocator require minSdkVersion 21+.
  Set this in android/app/build.gradle under defaultConfig:
    minSdkVersion 21
-->
